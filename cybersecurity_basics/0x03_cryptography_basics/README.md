SHA-1 Password Hashing Script

This project demonstrates a Bash script that hashes a password using the SHA-1 algorithm. The script accepts a password as an argument, computes its SHA-1 hash, and stores the result in a file named 0_hash.txt. The purpose of this project is to illustrate basic cryptography operations and handling of password hashing in a simple, automated way.
